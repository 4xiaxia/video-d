# 2026-06-06 工作记忆

## 第二步文稿/板书 rows JSON 入口修复

- 用户现象：第二步“生成文稿板书内容”的对话里出现 `Bad escaped character in JSON at position 263 (line 1 column 264)`。
- 对应关系已核对：
  - 界面第二步只生成候选 `rows`。
  - `rows[].voiceText` 是讲解文稿候选。
  - `rows[].boardSlice` 是 C 素材候选，也就是课堂板书内容候选。
  - 用户确认应用后，compiler 才把 rows 编译成正式 `spokenScript` / `boardPlan`，再由 store 写入正式 `scriptText` / `boardLayout`。
- 根因定位：
  - 报错发生在模型输出进入 rows 前的网关 `JSON.parse(jsonText)`。
  - 模型可能把 LaTeX 写成 JSON 字符串里的单反斜杠，如 `\left`、`\frac`、`\div`，其中部分会触发 `Bad escaped character`，部分会被 JSON 控制字符吃坏。
- 已改：
  - `scripts/script-agent-rows-contract.mjs` 新增 `parseJsonWithMathStringEscapes()`，只修复 JSON 字符串内部明确数学/LaTeX 命令反斜杠。
  - `vite.config.mjs` 的本地 `/api/agent/script-board` 使用共享 parser。
  - `scripts/zeabur-server.mjs` 的 Node/部署 `/api/agent/script-board` 使用共享 parser。
  - `scripts/check-script-agent-rows-contract.mjs` 加入未转义 LaTeX 反斜杠回归，并把 Node 路径改成优先 portable runtime、否则 `process.execPath`。
- 验证：
  - 直接 Node 复现未转义 `\left/\frac/\div`：通过，rows 保留 LaTeX 并编译到 `B1/C1`。
  - 直接 Node 验证合法 JSON `\n`：通过，未被误修成字面反斜杠。
  - 直接 Node 验证未知非法转义：通过，继续拒绝进入老师讲稿/板书候选。
  - `npm run check:script-agent-rows`：通过。
  - `npm run typecheck`：通过。
- 下一棒：
  - 回到 `src/modules/boardSticker/mathBoardText.ts`，修普通 C 文本换行被压扁的问题。
  - 不要改 rows 合同；`boardSlice` 和用户 `\n` 仍是内容真相。

## 产品语义纠偏

- 夏夏指出：这类细节错不可原谅，产品开发不能只看到技术。
- 已纠正：
  - 不再使用 loose/宽松口径。
  - 第二步 JSON parser 的产品边界是保护老师讲稿/课堂板书中的数学表达。
  - 只修复明确数学/LaTeX 反斜杠和数学定界符。
  - 未知非法转义必须暴露错误，不能偷偷进入候选内容。
- 后续纪律：
  - 每次修第二步或 C 板书链路，都要先讲清产品含义：老师讲什么、黑板写什么、学生看见什么。
  - 第二步 JSON 修复不叫“容忍模型错误”，而是保护课堂数学表达；未知非法转义必须失败。

## 舞台内样式分家

- 用户边界：画布是画布，其他是其他；先把画布内元素样式单独成册。
- 已改：
  - 新增 `src/stage.css`，把标签、题目、板书、金手指、舞台工具栏和 standalone 舞台壳子规则从 `src/styles.css` 拆出。
  - `src/main.tsx` 接入 `stage.css`。
  - `scripts/check-board-boundaries.mjs` 同时读取 `styles.css` 与 `stage.css`，并守门“舞台样式不得回流到全局样式表”。
- 验证：
  - `npm run check:board-boundaries`：通过。
  - `npm run typecheck`：通过。
- 下一棒：
  - 继续收标签 / 题目 / 板书的尺寸来源，判断是否统一到与画布同比例的唯一真相。

## 题图入口紧凑化

- 用户反馈：
  - 第 1 步题图上传区占位过大，希望改成“左边图缩略，右边按钮上传”的入口。
  - 同时提醒：不要为了样式 `div` 套 `div` 套很多层，别套娃。
- 已改：
  - `src/components/ProblemUploadPreview.tsx`
    - 上传控件从 `Upload.Dragger` 改成普通 `Upload`。
    - 空态 / 有图态统一成紧凑 `problem-upload-rail`，左缩略右按钮。
    - 保留原 `beforeUpload -> onImportProblemImage` 链路。
  - `src/styles.css`
    - 新增 `problem-upload-*` rail 样式，删掉旧大拖拽框样式。
    - 题图缩略使用 `object-fit: contain`，避免因为卡片尺寸裁图。
- 验证：
  - `npm run typecheck`：通过。
  - `npm run check:board-boundaries`：通过。
  - `npm run smoke:pending-new-problem`：通过。
  - Playwright 实测空态 / 有图态 rail 高度稳定为 `104px`。
- 后续纪律：
  - 这块保持紧凑工具位，不要回滚成大面积拖拽海报位。
  - 做 UI 时优先扁平结构，能一层解决就不加第二层。

## 标签分片容器自适应

- 用户意图：
  - 四区标签不是孤立装饰，而是对应“题目 / 分析 / 解答 / 总结”四个分片容器的句柄。
  - 容器大小按真实板书内容面积决定，简单规则就是内容 bbox 外加 `5px` 留白。
  - 交互边界必须守住：只能按住标签 pill 才移动，不能让整个容器可拖，否则会和板书文字拖拽撞车。
- 已改：
  - `src/modules/canvasStage/coursewareZoneLayout.ts`
    - 从 `stage-problem-text` 和 `board-text-sticker--zone-*` 的运行态 bbox 生成四区分片框。
  - `src/components/DrawboardStage.tsx`
    - 渲染动态分片框与标签 pill。
    - 标签 pill 支持本地拖动预览，但不写回任何业务字段。
  - `src/components/BoardTextSticker.tsx` / `src/components/CStickerFrame.tsx` / `src/components/AutoHandwritingLayer.tsx`
    - 给 C 板书挂分区 class，供舞台测量。
  - `src/components/CanvasRecordingSurface.tsx` / `src/modules/canvasStage/drawCoursewareStageFrame.ts`
    - 录制底图同步消费同一份分区布局真相。
  - `src/stage.css`
    - 分片容器外框 `pointer-events: none`。
    - 标签层级高于板书，避免误命中。
- 关键验证：
  - `npm run typecheck`：通过。
  - `npm run check:board-boundaries`：通过。
  - 本地 Playwright `standalone=drawboard-core`：
    - 拖 solution 标签后，标签位移 `x:+51 y:+18`
    - 板书在拖标签期间位移 `x:0 y:0`
    - 板书自己仍可拖动 `x:+40 y:+13`
    - 截图：`.tmp-ui-smoke/zone-label-drag-2026-06-06T06-12-59-865Z.png`
- 下一棒：
  - 继续把题目区 bbox、录制底图、Konva proof 统一到同一套尺寸口径。
  - 当前标签拖拽只是运行态预览，不要误报成已做 manual override。

## 右栏职责表容器收口

- 用户反馈：
  - `A/B/C 控制层职责表` 展开后容器没收住，像把右侧 inspector 当成长页在铺。
- 已改：
  - `src/styles.css`
    - `inspector-stack` 固定单列宽度。
    - 职责表 collapse 锁回 `width: 100%` / `min-width: 0`。
    - 展开内容改在真实 `.ant-collapse-body` 里内滚。
    - 标题、字段和 tag 全部允许断行，避免横向撑爆。
- 验证：
  - `npm run typecheck`：通过。
  - `npm run check:board-boundaries`：通过。
  - 本地 Playwright 实测：
    - 右栏无横向溢出，`scrollWidth == clientWidth == 294`
    - 职责表内容进入自身滚动容器
    - 下方 `画布变量 / 录屏舞台`、`C 默认字体 / 当前工程` 回到首屏可达
    - 截图：`.tmp-ui-smoke/inspector-responsibilities-2026-06-06T06-37-32-344Z.png`
- 后续纪律：
  - 这类右栏长文面板默认按“单列宽度 + 自身内滚 + 可断行”处理。
  - 右栏容器规则继续留在 `src/styles.css`，不要混到 `src/stage.css`。
