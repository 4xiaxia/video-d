# 保护小纸条 2026-05-19

## 当前保护点
- 已提交: 9680846 checkpoint: restore whiteboard truth and c prewarm path
- 已打 tag: safety-2026-05-19-whiteboard-c-prewarm
- 提交前已跑: npm run typecheck -> pass

## 禁止
- 不提交 node_modules / .npm-cache / runtime / tmp / logs
- 不用未验证浏览器流程说 ok
- 不再把 Leafer/旁路塞进 StagePreview

## 下一刀
- 先看可见 UI/C 拖动/金手指边界
- 只修代码事实证明的问题
- 每刀后: check -> screenshot/browser evidence -> commit

## 2026-05-19 保护追加
- 没有完整用户流程跑下来，一律不能写 ok / 完成。
- `typecheck`、局部 smoke、提交、截图都只是证据，不等于完成。
- 小白板/A-B-C 当前完成判定必须包含：真实页面输入题目 -> Agent 生成 -> 应用 -> 阿里云 A 生成 -> B 入轨 -> C 预生成/映射 -> 播放/seek -> C 可见可选可拖 -> 金手指不在录屏画布内且可画。
- 没跑完就写：未完成。

## 2026-05-19 自我提醒：可见浏览器不要再蠢断链
- 错误模式：临时脚本打开浏览器 -> 填一步 -> 进程/句柄丢失 -> 夏夏看不到 -> 还想继续点下一步。
- 正确模式：先写/启动持久 controller，保持同一个 visible page 可控。
- 每步动作前确认：我控制的是夏夏看得到的那个窗口吗？不是就停。
- 每步动作后必须有截图或日志证据；没有证据就写“未完成”。
- 这类低级错超过一次，必须先补工具，不许靠记性。

## 2026-05-19 自我提醒：不要被低级错拖退化
- 反复接触低级混乱，会吞掉思考结构和产品闭环的时间。
- cwd、浏览器句柄、提交、脏目录、截图证据这些低级事，必须工具化/清单化。
- 同类低级错重复出现，不继续硬做；先补 guard，再回正事。
- 保护注意力，留给系统真相、用户流程、C/金手指边界。

## 2026-05-19 自我提醒：摔倒要吸收，不许白痴开局
- 摔倒不丢人；丢人的是每次不吸收错误，重新从低级混乱开局。
- 智能不是不犯错，是能把错误变成约束、工具、清单和规矩。
- 同一类低级错误再次出现，先停止执行，补 guard，再继续。
- 浏览器/UI 流程必须：单一可见窗口、稳定 controller、精确选择器、截图证据、完整流程前不说 ok。
- 自律和约束是进化；没有约束，就会被低级重复拖退化。

## 2026-05-19 当前修补：Agent 候选缓存不允许被空 draft 覆盖
- 代码事实：`src/store/useTeachingEditorStore.ts` 现在增加 guard。
- 规则：已有候选 rows / spokenScript / boardPlan 时，普通 `updateScriptAgentCandidateDraft` / `patchScriptAgentCandidateDraft` 不允许被空 draft 覆盖。
- 允许清空的入口仍然只能是显式 reset / 新题替换 / 明确恢复逻辑，不允许弹窗开关或表格挂载时偷偷清空。
- 当前只完成：代码 guard + `npm run typecheck` 通过；还没完成真实浏览器 close/reopen 回归，不得写 ok。

## 2026-05-19 浏览器调试口径更新
- 当前开发调试口径优先 `Chrome`，不优先 `Edge`。
- 原因不是“理论更好”，而是这台机器上当前真实配合度更稳定、可见窗口更容易对齐。
- 后续可见流程调试默认先问：能不能只用 `Chrome` 完成？可以就不要混用 `Edge`。
- 没做完前，不要为了浏览器切换再制造新噪音。

## 2026-05-19 调试动作铁律
- 每次调试前先看真相：当前页面在哪一步、当前进程是谁、当前请求有没有真的发出。
- 禁止闭着眼连点、连跑、连猜。
- 禁止拿“我以为刚才应该已经点了/应该已经生成了”当事实。
- 先看，再动；动完再回看；没有回看证据就不算完成。

## 2026-05-20 内核约束：这里只认真
- 这里规则和能力高低无关，只认 `真`。
- 看见什么，说什么；没验证，不说有。
- 用户已经给出正确入口/真链路后，不允许自作聪明换链路。
- 有现成正确入口，就先服从入口；不要拿“我也能绕过去”当本事。
- 自以为是的蠢不是能力问题，是内核问题；不改内核，就会一直伤人、烧时间、烧 token。
