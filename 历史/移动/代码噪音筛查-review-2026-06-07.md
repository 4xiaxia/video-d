# 代码噪音筛查 review - 2026-06-07

## 范围

- 目标：先找出过期代码、旧链路残留、CSS 可合并/可清理点。
- 本轮边界：只做 review 与记录，不删除、不迁移、不改业务行为。
- 依据：当前真相源与代码实证共同判断；`历史/`、`src/_deprecated/`、旧文档批注不直接当活链路。

## 当前底层逻辑与不变边界

1. 当前主业务链：用户题目 -> 第 1 步题文确认 -> 第 2 步 rows 文稿/C 素材 -> 第 3 步 TTS/板书预览 -> 第 4 步舞台播放、C reveal、金手指、录制。
2. A/B/C 边界：A 是语音主时钟；B 管时间窗口；C 读 `boardSlice`/timeline clip 内容、站位、字号、宽度、速度等字段渲染。
3. 当前 C 文本主线：普通多行 `boardSlice` -> 保留用户 `\n` -> 手写字体文本渲染 -> 按字符 reveal -> DOM 预览与 Konva content canvas 录制同源消费。
4. 当前生产主舞台仍是 `StagePreview -> LegacyStagePreview -> DrawboardStage -> AutoHandwritingLayer`；`LegacyStagePreview` 名字旧但仍是现任生产链，不可按名字删除。
5. Konva 是方向，但公共入口尚未全切；`KonvaProofPage` / `KonvaRecordingSurface` 属 proof / 备用迁移入口，不能直接接主链。
6. 样式边界：`src/styles.css` 管页面壳子/时间轴/面板/非舞台区域；`src/stage.css` 管画布内元素、标签、题目、C 板书、金手指和舞台工具栏。

## 发现清单

### A. 高优先级：活链路里的过期依赖/旧框架尾巴

#### A1. 第三步预览卡仍依赖 tldraw

- 位置：`src/components/BoardPreviewCard.tsx`
- 实证：
  - `import { createShapeId, Editor, Tldraw, toRichText } from 'tldraw';`
  - `import 'tldraw/tldraw.css';`
  - `import { resolveTldrawStageSize } from '../modules/tldrawStage/abcToTldrawShapes';`
  - `<Tldraw hideUi ... />`
- 活链路：`src/components/VoiceWorkspace.tsx` 仍渲染 `<BoardPreviewCard draft={layoutPreviewDraft} stageCanvas={stageCanvas} />`。
- 判断：这是活代码里的 tldraw 尾巴，不能直接删；应作为后续“第三步预览卡去 tldraw 化 / 换 Konva 或 drawboard 预览”的独立工作单元。
- 风险：保留 `tldraw` dependency、`src/modules/tldrawStage/abcToTldrawShapes.ts` 被迫保留；会误导后续 agent 以为 tldraw 仍是当前舞台底座。

#### A2. `src/modules/tldrawStage/abcToTldrawShapes.ts` 大部分已过期，但 `resolveTldrawStageSize` 仍被活入口引用

- 位置：`src/modules/tldrawStage/abcToTldrawShapes.ts`
- 实证：`BoardPreviewCard` 只使用 `resolveTldrawStageSize`，但该模块还保留 `syncAbcStageToTldraw`、`TL_STAGE_SHAPE_IDS` 等旧舞台同步逻辑。
- 判断：模块内存在“部分活、部分旧”的混杂。不能整体删除；后续应先替换 `BoardPreviewCard` 的尺寸 resolver，再将该模块整体挪入 deprecated 或拆出无 tldraw 依赖的通用 stage size helper。
- 风险：`syncAbcStageToTldraw` 这类旧主舞台概念容易被误复活。

#### A3. `package.json` 仍保留 `tldraw` 依赖

- 位置：`package.json`
- 实证：`dependencies.tldraw` 仍存在。
- 判断：当前仍被 `BoardPreviewCard` 和 `src/_deprecated` 文件 import；在 A1/A2 未处理前不能删 dependency。
- 清理顺序：先换掉 `BoardPreviewCard`，再处理 `src/modules/tldrawStage`，最后移除 `tldraw` dependency 并跑 typecheck/build。

### B. 中优先级：已隔离旧代码与全局样式残留

#### B1. `src/_deprecated` 内有 tldraw proof/stage 死文件

- 位置：`src/_deprecated/TldrawProofPage.tsx`、`src/_deprecated/TldrawStagePreview.tsx`
- 实证：目录只含这两个文件；当前 `src/main.tsx` 没有 `standalone=tldraw-proof` 路由，公共入口也只 re-export `LegacyStagePreview`。
- 判断：已隔离，当前不影响运行；但若目标是“旧代码清干净”，这里仍是显式旧代码库存。
- 建议：若仍需可逆证据，保留在 `src/_deprecated`；若要彻底降噪，下一步把它们移入 `历史/` 或删除，并同步清理样式与依赖。

#### B2. `src/styles.css` 末尾仍保留 tldraw proof/stage 样式

- 位置：`src/styles.css` 2461-2596 行附近。
- 实证：`.tldraw-proof-*`、`.tldraw-stage-*`、`.tldraw-stage-modal` 等样式仅对应 `src/_deprecated` 旧组件；活组件搜索未发现这些 class。
- 判断：这是比 `src/_deprecated` 更明确的页面样式噪音，因为全局 CSS 仍加载到当前应用。
- 建议：可作为第一批低风险清理候选；删除前跑 `npm run typecheck` 与 `npm run check:board-boundaries`。

### C. 低优先级：名字 legacy 但仍有边界价值，不应误删

#### C1. `LegacyStagePreview` 名字旧但仍是生产链现任

- 位置：`src/components/StagePreview.tsx` -> `src/components/LegacyStagePreview.tsx`
- 实证：`StagePreview` 当前只 `export { StagePreview } from './LegacyStagePreview';`。
- 判断：不可删、不可按名字改入口；真正切 Konva 主舞台前它仍是生产链。

#### C2. `isLegacyPreviewMessage` 是本地聊天记录过滤器

- 位置：`src/components/AgentReviewCard.tsx`
- 判断：legacy 命名表示过滤旧消息格式，属于兼容/清洁逻辑，不是死代码。

#### C3. `stripLegacyTags` / `legacyBoardMarkerPattern` 是输入兼容边界

- 位置：`src/modules/scriptAgentTable/normalizeScriptAgentTableDraft.ts`、`src/modules/timeline-factory/splitScriptIntoTtsSentenceUnits.ts`、`scripts/script-agent-rows-contract.mjs`
- 判断：用于清洗旧标签或兼容旧板书标记，且有回归脚本约束；不应当作噪音直接删。

#### C4. `data-legacy-anchor` 是测试/定位兼容锚点

- 位置：`src/components/boardClipInspector/BoardClipInspectorSections.tsx`
- 判断：这是兼容旧锚点的 DOM 标记，不影响业务逻辑；如果要清理，应先确认所有脚本/外部自动化不再依赖旧 anchor。

### D. 备用/旧路模块：不能回主路，但也不能盲删

#### D1. `renderBoardTextStickerImage` / `renderBoardMathStickerImage`

- 位置：`src/modules/boardSticker/renderBoardTextStickerImage.ts`、`src/modules/boardSticker/renderBoardMathStickerImage.ts`、`src/modules/boardSticker/index.ts`
- 实证：当前普通 C 主路不应调用它们；`scripts/check-board-boundaries.mjs` 已禁止普通 C 录制/预览回到 PNG 主路。
- 判断：它们是备用/旧路模块。普通 C 不许回用；复杂公式/备用路线未完全清算前不建议直接删。
- 注意：文件内 `fillText` 不等于违规；违规的是 `AutoHandwritingLayer` 普通 C content 录制主路重新用 `fillText` 或 PNG。

#### D2. `KonvaRecordingSurface`

- 位置：`src/components/KonvaRecordingSurface.tsx`
- 判断：proof / 备用迁移入口，不是生产主链。它不是“过期代码”，但当前不应误接公共入口；后续 Konva pilot 可参考。

### E. CSS 可合并/可清理点

#### E1. tldraw 样式可清理

- 位置：`src/styles.css` 2461-2596 行。
- 风险等级：低到中。低是因为活组件不用；中是因为如果仍想通过手工 import `_deprecated` 页面做考古，会丢样式。
- 建议：如果目标是当前应用降噪，优先删除这些全局样式；若保留 `_deprecated` 可逆证据，应把样式也移入历史说明或 deprecated 私有 CSS，而不是继续留在全局 `styles.css`。

#### E2. standalone header/grid label 样式重复

- 位置：
  - `.c-standalone-page__header` 与 `.drawboard-standalone-page__header`
  - `.c-standalone-grid label` 与 `.drawboard-standalone-grid label`
  - `.drawboard-hybrid-prototype__float-body label` 与上述 label 样式重复
- 判断：可合并成共享工具类，例如 `.standalone-page-header`、`.standalone-control-grid label` 或通过组合选择器合并。
- 风险：这些是 prototype/standalone 页面，改动风险比主工作台低；但仍需浏览器 smoke，避免单体页布局变形。

#### E3. `settings-drawer-footer` 与 `inspector-stack__header` 属性相同但语义不同

- 位置：`src/styles.css` 551 行、1680 行附近。
- 判断：属性重复，但分属 settings drawer 与 inspector header；可以合并为工具类，但收益较低，容易降低语义可读性。本轮不建议优先动。

#### E4. `src/styles.css` 仍大且混杂，但已不是单一舞台样式问题

- 当前舞台内主要样式已迁到 `src/stage.css`。
- 剩余可继续分层：standalone/prototype 样式、agent/workspace 样式、inspector 样式、timeline 样式。
- 建议顺序：先删明显 tldraw 残留，再合并 standalone/prototype 重复块，最后考虑按页面域拆 CSS 文件。

## 建议清理顺序

1. 清 `src/styles.css` 末尾 tldraw proof/stage 全局样式残留，并确认 `src/_deprecated` 不再需要全局样式。
2. 把 `BoardPreviewCard` 从 tldraw 承载迁到 Konva 或轻量 DOM/canvas 预览；这一步必须测第三步预览。
3. `BoardPreviewCard` 去 tldraw 后，处理 `src/modules/tldrawStage/abcToTldrawShapes.ts`：拆出通用尺寸 helper 或整体移入 deprecated。
4. 移除 `package.json` 的 `tldraw` dependency，跑 `npm run typecheck` / `npm run build` / 相关 smoke。
5. 再决定是否删除或迁移 `src/_deprecated/TldrawProofPage.tsx`、`src/_deprecated/TldrawStagePreview.tsx`。
6. 合并 standalone/prototype 的重复 CSS 类。

## 本轮验证

- `rg` 不在当前终端 PATH，已改用 PowerShell 与内置搜索交叉验证。
- 已用 PowerShell 扫描 `src` / `scripts` 下 `tldraw`、`Tldraw`、`renderBoardTextStickerImage`、`fillText`、`constrainYPercentToZone` 等关键词。
- 已用 CSS 类扫描找出未被活 TS/TSX 引用的 `.tldraw-*` 残留和重复 CSS block。
- 本轮未改业务代码，未跑 typecheck；后续实际删除/合并时必须补跑最小验证。

## 结论

旧代码没有完全清干净。最明显位置是：

1. 活链路第三步预览卡仍有 tldraw：`src/components/BoardPreviewCard.tsx`。
2. tldraw 模块混杂保留：`src/modules/tldrawStage/abcToTldrawShapes.ts`。
3. 全局 CSS 末尾仍有 tldraw proof/stage 样式：`src/styles.css` 2461-2596 行。
4. 已隔离旧组件仍在：`src/_deprecated/TldrawProofPage.tsx`、`src/_deprecated/TldrawStagePreview.tsx`。
5. `package.json` 仍有 `tldraw` 依赖，原因是 A1/A2 未拔干净。

CSS 可合并重点是 standalone/prototype 类；不要优先合并语义不同的 settings/inspector header。