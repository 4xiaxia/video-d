---
记录时间: 2026-06-02-180830
性质: 增量阅读总入口 / 第四步偏移点清单 / 真相锚点 / 全仓 X-ray 实证
状态: 当前生效（newest first）
作者: 阿圆（基于代码 grep + import 链实证，非记忆、非猜测）
最后动作: 全仓 X-ray 压实 + 死代码隔离 + 数学手写路由闭环 + 三次 typecheck 通过
规则: 以后接手先读本页。本页是增量入口，不要再全文重读 28KB 文档 / 代码 / 12MB 会话。
---

# 真相路标：当前唯一入口

> 一句话：前 1-3 步是干净的，乱的只在第四步舞台层的"文字描述"和"工具纪律"，代码主链其实是稳的。
> 本页是项目根目录唯一路标。`历史/` 是杂物间，不用管。

---

## 🩺 病灶诊断书（2026-05-31 交付日，全部 grep 实证）

> 交付结论：**程序可交付**。typecheck ✅ 通过，主链能跑。下列四条是已知技术债，今天不动，交付后专项开刀（一条一刀，走完整闭环）。

| # | 病灶 | 实证证据 | 影响交付? | 处置 |
|---|---|---|---|---|
| 1 | **tldraw 没清干净** | 主链(App)已不碰 tldraw；唯一活尾巴=`BoardPreviewCard.tsx`(第三步预览卡)仍 `import 'tldraw'`+用 `resolveTldrawStageSize`。~~`TldrawStagePreview.tsx`(384行)~~ **已于 2026-06-02 挪至 `_deprecated/`**；~~`TldrawProofPage.tsx`~~（243行）**已于 2026-06-02 挪至 `_deprecated/`，main.tsx 路由同步摘除**。 | ❌ 不影响(能跑) | 交付后：BoardPreviewCard 换 Konva；死文件已挪 _deprecated/（可逆） |
| 2 | **全局定义没做·CSS散** | 全挤 `src/styles.css` 一个文件 **~2479行**（实测校正，原称2917）；**px硬编码~535处**（实测校正，原称604）；CSS变量出现在 91 行（原称13个变量名，口径不同）；rem/% 混用属实。这是"一动就炸"的根。 | ❌ 不影响(能跑) | 交付后：建全局令牌层，散值收敛到 token |
| 3 | **金手指/录屏没解耦** | 金手指`GoldenFingerCanvasLayer`、录屏`modules/stageRecorder`本是独立模块，但被 props 链焊进主舞台(Legacy→Drawboard→ToolOverlay)。本质是overlay外挂层，不该耦合ABC。**X-ray 补充**：`teachingProject.ts` 定义的 `goldenFingerOverlays` 字段是空壳（无活跃写入），GoldenFingerCanvasLayer 用内部 state，录屏靠 DOM querySelector。边界正确但不接 store。 | ❌ 不影响(能跑) | 交付后：用 context/外挂层解耦，金手指录屏脱离主舞台props链 |
| 4 | **参数覆盖链缺中层** | 配置真相唯一(`config/defaultConfig.ts`)✅；`mergeConfig`用`{...default,...saved}`覆盖不改源✅；但只有**两层**(默认+用户)，缺独立的 **agent编排中层**。目标链：底层默认 < agent编排 < 用户微调(覆盖而非改业务代码)。 | ❌ 不影响(能跑) | 交付后：在 store 补 agent 覆盖层，凑齐三层权限链 |

> 共同原则（交付后做四个专项时都守）：**覆盖/叠加，不侵入源**。CSS叠token、参数叠覆盖层、金手指叠overlay——都是同一个"叠加不改源"的哲学，对应"大道至简·多米诺骨牌"。

---

## 🧬 埋线地图（藏在里面的东西 · 这才是工程的命）

> 元规矩（最重要，先读）：**这个项目的线早就埋好了。读线，不许猜。**
> 想当然按市面通用做法猜 = 必错，因为这套设计的每个字段都有用意、边界、上下游。
> "能跑"是最大的假象——凭印象改，程序照样不报错、照样能交付，但藏在里面的贯穿链已被悄悄拧断，等问题浮上来已经塌了一片。
> 抓改动是否"有教养"，本质就是抓：它有没有去读埋好的线，还是在猜。

### 🎮 Agent 画布控制点（操纵杆清单 · 以后 agent 控制画布只动这些字段，不碰渲染代码）

agent 让 C 板书"画在哪、什么样、怎么动"，只写字段，不碰渲染层：

| 操纵杆字段 | agent 写它 = 控制什么 | 落点 |
| --- | --- | --- |
| `section` | C 落哪个区（开场读题/分析题目/解题环节/梳理总结） | 四区定位 |
| `chainKey` | A/B/C 唯一身份（由 section 归一化生成，不手填） | 贯穿编译/timeline/标签 |
| `boardSlice` | C 写什么内容 | → C 素材候选 → TimelineClip.label |
| `voiceText` | A 这一句老师怎么讲 | → spokenScript → TTS |
| `xPercent/yPercent` | C 在区内精确站位 | 画布坐标(×画布宽高，不写死px) |
| `widthPercent` | C 多宽 | 排版 |
| `fontSize` | C 多大 | 字号 |
| `drawSpeed` | C 逐笔多快（像真人书写） | 演绎节奏 |
| `revealStartMs/EndMs` | C 何时开始/写完显现 | 逐笔时机 |
| `startMs/endMs`(B) | C 何时出现/消失（由 A 真实时长生成） | 寿命窗口 |

核心边界：**agent 控制面=写字段值（数据层）；画布渲染层=读字段自动呈现。两层解耦，agent 永不碰渲染代码。** 这就是"agent 友好"的本质，也是 tldraw 要退、Konva 要上的根本原因（tldraw 的 shape/store 模型挡着 agent 直接控制这些字段）。

### 🔗 ABC 贯穿链（代码 ↔ 文档已对照咬合，前 1-3 步可信）

```
rows 表格(骨牌第一张, ScriptAgentTableEditor.tsx)
  ├─ voiceText ─→ A候选语音 ─→ spokenScript ─→ TTS拆句 ─→ A轨真实时长 ┐
  ├─ boardSlice ─→ C候选内容(先) ──────────────────→ B寿命窗口(后)←┘ ─→ TimelineClip
  └─ section ──→ chainKey身份 ──→ 决定 C 落哪个画布区 + 贯穿编译/timeline/标签
```
- 时序铁律（藏在编译逻辑里，看页面看不出）：**C内容先定，但 B 寿命窗口必须等 A 语音真实时长回来才能算。A 是主时钟。**
- 四区 boardSlice 规则（defaultConfig.ts prompt 写死）：开场读题必须留空；分析/总结可填；解题环节填核心算式/变形/答案。
- chrome 边界：`drawCoursewareStageFrame.ts` 四区标签只画底板（render-only），不读不改 ABC。chrome=外壳，C=演员，互不污染。
- **数学符号双路由（X-ray 实证 2026-06-02）**：`speechText/aliyunMathSpeechText.ts`(耳朵路，÷→"除以") ↔ `boardSticker/boardTextDisplayRoute.ts`(眼睛路，÷→显示为÷)，两条路分属独立模块，共用一个 `normalizeBoardMathText` 基础归一化但路由分叉。手写转换表 `mathBoardText.ts:71-91` 已含 17 条 LaTeX→Unicode 映射(`\times→×`/`\div→÷`/`\angle→∠`/`\pm→±`/`\perp→⊥`等)，补全于 2026-06-02 症状B修复。`stripSimpleBoardMathDelimiters` 剥 `$...$` 保护壳、`repairCommonLatexEscapeDamage` 修断裂 `\frac` 后交阿里云——两条防线都在，不能删。
- 文档真相源：`ABC字段函数前端映射表.md` 主链映射仍有效，与代码咬合。

### 📍 埋点（以后做配置面板 / 接画布控制，搜这里）

- `@埋点-agent提示词配置面板`：`config/defaultConfig.ts` 的 `scriptAgent.promptSystem / promptUserTemplate / outputContract`。现在编辑入口在 `AppSettingsDrawer`，未来做独立"提示词设置"面板。
- `@埋点-rows字段源头`：`scriptAgentTable/ScriptAgentTableEditor.tsx`。这是整条参数贯穿链的源头（骨牌第一张），五列 section/stepLabel/boardSlice/voiceText + 隐藏 chainKey。
- `@埋点-画布操纵杆`：上面的操纵杆清单。Konva 承接时，必须保证这组字段照样能被 agent 写、被画布读。

---

## 0. 增量阅读入口（省 token 的关键，先读这里）

接手时**只读本页**就够。需要细节再按下面指针去对应文件，**不要全文重读**：

| 想知道什么 | 去哪（只读相关段，不全文重读） |
| --- | --- |
| 当前该按什么口径施工 | 本页第 2、3 节 |
| 主舞台真实路径 | 本页第 2 节（已 grep 实证，可直接信） |
| 哪些是死代码 / 哪些是活的 | 本页第 4 节 |
| chrome / zone 是不是乱造的 | 本页第 5 节（平反） |
| 历史每一刀干了什么 | `CHANGE_TREE变更树.md`（**在根目录，不在 `历史/`**；2026-06-02 已接续，断树已修） |
| 上一轮的护栏铁律 | `历史/当前保护小纸条_2026-05-19.md` |
| 字段映射真相 | `ABC字段函数前端映射表.md`（主链映射仍有效） |

---

## 1. 这次的根因（为什么会被搞乱）

- **变更树断了**：`历史/CHANGE_TREE变更树.md` 最新一刀停在 `2026-05-06`，但文档改到 `05-30/05-31`。中间 20 多天的改动**一刀都没登记**。
- **不写树 → 后人没接力线索 → 只能瞎猜代码 → 把干净的东西当脏东西铲**。
- **git 不可信**：只有 1 条 `backup` 提交（2026-05-30 09:11），熊孩子不按"每刀 commit"纪律，烂账被一个 backup 糊住了。
- **每次全文重读**：上下文动辄几十万 token，是因为没有增量入口。本页就是来终结这件事的。

---

## 2. 第四步主舞台真相（已 grep + import 链实证，可直接信）

当前**实际在跑**的主舞台路径：

```
App.tsx
 └─ StagePreview.tsx          仅 re-export（是"唯一公共入口"切口，勿删、勿当多余）
     └─ LegacyStagePreview.tsx    ← 真正的主舞台实现（名字叫 Legacy，但它是现任）
         ├─ DrawboardStage.tsx        四区标签已落地：题目/分析/解答/总结（104-107 行）
         │   └─ coursewareChrome.ts   系统字体外壳层（chrome=真概念）
         └─ AutoHandwritingLayer      C 层逐笔手写板书（boardSlice→TimelineClip）
```

- 录制契约：`StageRecordingCanvases { base, content, overlay }`，走 `useCanvasRecorder`。
- 逐笔显现：`drawSpeed` + `reveal*` 控制。
- 金手指：`goldenFinger overlay strokes` 是 overlay 顶层人工补救，不写 ABC base。✅边界正确。

---

## 3. tldraw 全线退场（不是当前框架，别再当底座）

- ~~`TldrawStagePreview.tsx`（384 行）~~ **已于 2026-06-02 挪至 `src/_deprecated/`**，全仓零 import，可逆隔离。
- ~~`TldrawProofPage.tsx`~~（243 行）**已于 2026-06-02 挪至 `src/_deprecated/`**，main.tsx 路由同步摘除。
- `abcToTldrawShapes.ts` = 遗留退场线（仍被 `BoardPreviewCard.tsx` 引用 `resolveTldrawStageSize`，不能删，等 BoardPreviewCard 去 tldraw 化）。
- `BoardPreviewCard.tsx` 里仍有 tldraw 风格 size 尾巴（`import { Tldraw, ... } from 'tldraw'` + `import 'tldraw/tldraw.css'`）= **活代码里的残留，待拔**。
- 目标承载 = **Konva**，目前只在 `KonvaProofPage`（`?standalone=konva-proof`）验证，**尚未接主链**。
- 为什么换：tldraw 太重、shape/store 模型对 agent 程序化逐笔控制不友好；Konva 是裸 canvas，agent 想怎么画就怎么画，录屏也更直接。

---

## 4. 死代码判断（某 agent 大面积误判，差点误删）

| 组件 | 某 agent 判定 | 实证结论 | 证据 |
| --- | --- | --- | --- |
| ~~`TldrawStagePreview`~~ | 死 | ✅ **真死，已挪 `_deprecated/`**（零 import） | grep 全仓无引用，2026-06-02 隔离 |
| ~~`TldrawProofPage`~~ | — | **已挪 `_deprecated/`**（仅 main.tsx 旧 standalone 路由引用） | 2026-06-02 路由摘除+文件隔离 |
| `ScriptBoardSummaryStep` | 疑似死 | ❌ **活的** | `createAssetWorkflowSteps.tsx:105` |
| `ProblemUploadPreview` | 疑似死 | ❌ **活的** | `ProblemWorkspace.tsx:24,80` |
| `CurrentProjectBoardFontInspector` | 疑似死 | ❌ **活的** | `InspectorPanel.tsx:14,32` |
| `BoardControlResponsibilitiesPanel` | 疑似死 | ❌ **活的** | `InspectorPanel.tsx:12,30` |

教训：那个 agent 只看了 `App.tsx` 一层，没追 `workflow/` 和 `InspectorPanel` 子树就判死刑。**删之前必须 grep 全仓引用。**

---

## 5. 被冤枉的术语，平反（不许再当噪声铲掉）

- **`chrome`**：真概念，不是乱造的。指**非手写的系统字体外壳层**（四区标签 / 题目区 / 结构提示）。对应代码 `src/modules/canvasStage/coursewareChrome.ts`、`createCoursewareChromeStyleVars()`。认知图 §5.1 用得完全正确。
  - 噪声是认知图 §2.3（131/138 行）那两条吐槽批注"这里开始飘了→什么是chrome"——那是对正经术语的误解倒灌进文档，**要清的是吐槽，不是 chrome**。
- **`zone`**：仅 CSS 类名（`zone-card` / `zone-stage` / `zone-inspector`），布局样式，2026-04-25 那刀为"浅绿块/淡紫块"区域底色加的。"不引入 zone 新概念"是**正确护栏**，不是矛盾。
- **铁律（来自 continuity skill）**：不要把一时的修复冲动当成比已记录的有效边界更强。旧笔记说两层是有意为之，就保留两层，只澄清名字/写入权/生效时机。

---

## 6. 工具纪律偏移（要统一，别重复造）

- **imptokens 残骸**：`C:\Users\admin\.claude\plugins\imptokens\` 是某 agent 2026-05-31 手搓的（compress.py 已删，但 detect.py/integrate.py 还赖着，stats 全 0，从没真正压缩过）。正主是 `plugins\imptokens-pruner\`（有 plugin.json + 真实 repos 数据）。**用 imptokens-pruner，手搓那个待清理。**
- **上下文工具不统一**：上一轮长会话全程用 `lean-ctx`（MCP），但充值买的/认错记录指定的是 `imptokens-pruner`。需要夏夏定一个，别两套并存。
- **skills 都在**：`C:\Users\admin\.claude\skills\` 下 7 个 skill 完整（brainstorming/graphify/imptokens/xiaxia-anchor-marking/xiaxia-context-compression/xiaxia-continuity/zeta-remembering-anchors）。**没飞，可直接用。**

---

## 7. 安全提醒（别泄露）

- `settings.json` 和 `imptokens-pruner/config.json` 内有明文 API key。别截图外发、别提交进 git。建议挪环境变量或确认在 `.gitignore` 内。

---

## 8. 诚实标注（哪些已验证，哪些没跑通）

- ✅ 已验证（grep / import 链 / 读文件实证）：第 2、3、4、5、6 节全部。
- ⏳ 未跑通：graphify 尚未对本项目跑过（`历史/skills 要用啊/graphify/.graphify/` 那份是工具给自己生成的，不是本项目的）。想要客观全局图谱需另跑。
- ⏳ 未验证：完整用户流程（题目→Agent→应用→A生成→B入轨→C可见可拖→金手指→录屏）没真机跑，按保护小纸条铁律，不写"完成"。

---

## 9. 下次回来（最小继续动作，按优先级）

1. ⏳ 拔活链路 tldraw 尾巴：`BoardPreviewCard` 的 `import tldraw` + `resolveTldrawStageSize`（影响第三步预览，要测，换 Konva 承载预览）。
2. ⏳ 清 tldraw 模块尾巴：`abcToTldrawShapes.ts` — 等 BoardPreviewCard 去 tldraw 化后可挪。
3. ⏳ 清工具残骸：手搓的 `plugins\imptokens\` 挪 backups。
4. ⏳ 继续第3步 `voiceText→TTS→A轨真实时长` 走读与 `B寿命窗口生成` 验证。
5. ✅ **已完成 2026-06-02**：死代码隔离（TldrawStagePreview + TldrawProofPage → `_deprecated/`）、全仓 X-ray 字段实证、数学手写路由闭环验证、文档三份同步更新。

> 原话留底（夏夏）："参数贯穿，下一步的参数来源于上一步，所有环节一环扣一环，错位一个下面全错。" / "工具好好用，不是重复用，增量阅读不是重复阅读。"
