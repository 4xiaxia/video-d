# 2026-06-05

- 验证页面重影修复：`.canvas-recording-surface` 作为录制底板仍存在，但页面层 `opacity: 0`、`pointer-events: none`；DOM 侧保留 4 个可见标签，页面只显示一套题目/标签。
- 运行最小验证：TypeScript 检查通过；工作台冒烟通过；录制聚焦冒烟通过；生产构建通过。
- 纠正 B/C 文案漂移：`BoardClipInspector.tsx` 与 `BoardClipInspectorSections.tsx` 统一为“默认 C 写完继续留场”“B 只管上台和解锁后的下台截止时间”。
- 修复 `scripts/check-board-boundaries.mjs` 对 `runtime/node/node.exe` 的硬依赖，缺失时回退 `process.execPath`；复跑后暴露现存契约失败：`normalizeBoardFontUrl('')` 与边界检查期望不一致，需单独处理。
