- 进行上下文压缩前先写入长期项目记忆：当前断点是 C 板书降配与文档纠偏；主线为 `boardSlice` 普通多行文本保留换行 → 手写字体 → 逐字 reveal → 页面缩放版/录制全尺寸版同源消费；旧 SVG/逐笔轨迹路线冻结备用；待纠偏文档为《真相路标》《认知图》《ABC映射表》《CHANGE_TREE》。
- 验证页面重影修复：`.canvas-recording-surface` 作为录制底板仍存在，但页面层 `opacity: 0`、`pointer-events: none`；DOM 侧保留 4 个可见标签，页面只显示一套题目/标签。
- 运行最小验证：TypeScript 检查通过；工作台冒烟通过；录制聚焦冒烟通过；生产构建通过。
- 纠正 B/C 文案漂移：`BoardClipInspector.tsx` 与 `BoardClipInspectorSections.tsx` 统一为“默认 C 写完继续留场”“B 只管上台和解锁后的下台截止时间”。
- 修复 `scripts/check-board-boundaries.mjs` 对 `runtime/node/node.exe` 的硬依赖，缺失时回退 `process.execPath`；复跑后暴露现存契约失败：`normalizeBoardFontUrl('')` 与边界检查期望不一致，需单独处理。
- 修复板书字体 URL 契约：`normalizeBoardFontUrl(undefined)` 继续回退默认 HTTPS 字体地址；显式空字符串、非法 URL、非 HTTPS URL 统一归一为 `''`，不再偷偷回退外部字体服务。
- 同步右侧板书字体地址输入框：`BoardTypographyFields.tsx` 用空值合并 `??` 保留显式空字符串，避免把“用户清空字体地址”错误显示成默认外链字体 URL。
- 收口边界脚本真相：`check-board-boundaries.mjs` 对 speech-protected 简单算式的断言改为显示安全字形 `25x4=100`，与当前手写路由的 ×→x / ÷→· 显示归一保持一致；TypeScript 检查与板书边界检查均通过。
- 下一步规划收敛为“先冻结录制面真相，再小步接 Konva”：优先做 C 真相链路文档/锚点，其次录制弹窗全尺寸同源化，最后才处理页面缩放预览与交互层统一。
- 甲方确认降配：旧 SVG/逐笔轨迹路线冻结保留作备用；当前主线改为“手写字体 + 普通文本输入 + 逐字 reveal”，除去 SVG 依赖外，其余时序/留场/交互/录制规则全部继承原方案。
- 根据截图确认新缺陷边界：第三步板书内容与右侧 C 控制排版正确，页面/舞台写入错误；主因应查页面渲染层多行文本保留、lineHeight、文本块测量与标签自适应布局。
- 只读扫描后确认文档偏移集中在“逐笔/CSS reveal/Konva 迁移口径”几处：根目录《真相路标》《认知图》《ABC映射表》《CHANGE_TREE》需同步降配为“普通多行文本 + 手写字体 + 逐字 reveal”，并把 Agent 实时排版矫正明确降级为兜底层而非主链依赖。
- sub 批判评审结论：C 文本降配方案可通过但必须拆刀；更安全顺序是先只读定位换行丢失点，再抽纯文本 layout 并测试，页面/录制共用，随后同步变更树，最后拆分标签 bbox+3px 自适应与 manual override。
- 已把 C 文本降配实施步骤落到 4 个根目录真相文档：`CHANGE_TREE变更树.md`、`真相路标-当前唯一入口.md`、`认知图-核心逻辑动态图.md`、`ABC字段函数前端映射表.md`。核心护栏：先只读定位换行丢失点；layout 同源；标签 bbox+3px 与 manual override 分两刀；标签拖动不得写回 `boardSlice`。
