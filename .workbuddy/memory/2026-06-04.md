# 2026-06-04

## 板书留场语义校准

- 用户纠正第四步舞台问题2的产品语义：项目是直播仿板书在线解题，C 板书内容自然播放完成后应默认 stay 在画布上，不应 1 秒后消失。
- 当前 `STATIC_HOLD_DURATION_MS = 1000` 方案属于认知偏差：把“板书长期留场”误解成“播放结束短暂缓冲”。
- 正确语义：B 仍保留拖动控制能力；播放完的切片末尾有 lock 小图标；默认锁定 stay，点击 lock 解锁后可拖动 B 控制 C 的站场/显示时长。
- 已写入项目长期记忆 `D:\video-dev-cleanroom\.workbuddy\memory\MEMORY.md`，作为后续修复依据。

## 文档纠偏同步

- 已同步 `.claude/PROJECT_STATE.md`：标记问题2当前实现语义错误，待重做。
- 已同步 `.claude/PROJECT_COGNITION.md`：重写 ABC 生克关系，明确 reveal 完成 ≠ C 下台，默认 lock 留场，unlock 后 B 控制站场时长。
- 已同步根目录核心文档：`真相路标-当前唯一入口.md`、`认知图-核心逻辑动态图.md`、`ABC字段函数前端映射表.md`、`CHANGE_TREE变更树.md`、`落地项目开发说明明细文档-代码直敲版.md`。
- 统一口径：B 不再表述为“控制 C 自然消失”的轨，而是“寿命/站场控制轨”；C 自然播放完成后默认留场，解锁后才进入可控站场时长语义。

## 产品语境优先规则补充

- 用户明确强调：技术判断和代码调整必须先回到产品语境；本项目的“直播仿板书在线解题”语境决定了板书写完默认留在画布上。
- 已将“产品语境优先”补入用户级 `dev-conduct` skill：先判断真实产品常识，再决定技术修复。
- 代码方案倾向：用可选截止时间表达 C 是否下台；没有截止时间则默认 stay，有截止时间则到点 display 掉。

## doc-sync-mini 失败后手动接管文档同步

- `doc-sync-mini` 因 Max turns exceeded 失败，未把其结果视为完成。
- 手动接管后补写旧文档纠偏批注：`.claude/SUMMARY_2026-05-31.md`、`.claude/DELIVERY_CHECKLIST_2026-05-31.md`、`.claude/CHECKPOINT_2026-05-31_STAGE_ISSUES.md`、`.claude/ENGINEERING_LOG.md`。
- 同步修正遗漏口径：`认知图-核心逻辑动态图.md` 中 `startMs/endMs(B)` 改为“出现 / 进入可控站场状态”；`src/modules/scriptSegments/README.md` 明确 C 默认留场，不能简单理解成到 `endMs` 自然消失。
- 复查后，当前活跃核心文档中的旧问题2方案均已有 2026-06-04 纠偏入口；历史目录中的旧口径保留为历史，不作为当前施工依据。

## 问题2代码纠偏落地

- 已撤掉 `STATIC_HOLD_DURATION_MS = 1000` / 1 秒 static hold 错误实现。
- 新增 `TimelineClip.hideAtMs?: number`：未设置表示 C 默认 stay；设置后 playhead 到点隐藏。
- `AutoHandwritingLayer` 改为 `isBoardClipVisibleAtPlayhead(playheadMs, startMs, hideAtMs)` 过滤 C 可见性。
- `TimelineClipBlock` 末尾 pin 改为 lock/unlock 入口：默认 lock 不允许拖 B；unlock 写入 `hideAtMs = clip.endMs`，之后拖 B 控制隐藏截止时间。
- 同步 `tldrawStage` 历史适配层、规则文案、契约脚本、`PROJECT_STATE.md` 与 `CHANGE_TREE变更树.md`。
- 验证通过：`tsc --noEmit`、`check-timeline-window-contract.mjs`、`smoke-board-end-pin-visible.mjs`。
- `check-board-boundaries.mjs` 仍在 Courseware stage edge 检查处提前失败，属于独立边框约束问题，未纳入本轮修复。

## 手写字体基础符号拦截修复

- 问题：手写字体（平方乔木体）支持基础数学符号（≤≥≠≈√∈⊂∪→等），但画布路由把含这些符号的文本拦截到 formula（KaTeX），导致显示异常。
- 根因：`normalizeElementaryBoardHandwritingText` 只转换了一部分 LaTeX→Unicode（\times→× 等），漏掉了 `\le`→`≤`、`\ge`→`≥`、`\neq`→`≠`、`\approx`→`≈`、`\pi`→`π`、`\in`→`∈`、`\sqrt`→`√` 等常用命令。
- 这些未转换的 LaTeX 命令被 `HANDWRITING_STRUCTURAL_MATH_PATTERN` 拦截 → 路由到 formula。
- 修复：
  - 补全 20+ LaTeX→Unicode 转换（\le/\ge/\neq/\approx/\equiv/\pi/\infty/\sqrt/\in/\notin/\subset/\supset/\cup/\cap/\forall/\exists/\emptyset/\to/\rightarrow/\leftarrow/\leftrightarrow/\Rightarrow/\prime/\dprime/\sum/\prod）。
  - 扩展 `HANDWRITING_EXTRA_SYMBOLS`：新增 √∈∅→←↔⇒⊂⊃∪∩∀∃′″℃℉ 等基础符号。
  - 原则：字体支持的基础符号走手写路由；结构数学（\frac、多字上下标）仍走 formula。
- 验证：`tsc --noEmit` ✅。

## 代码去重 + 真相文件压实

- `mathBoardText.ts`：删除重复 `\\degree` 替换（第123行，06-02批第81行已有）；`HANDWRITING_EXTRA_SYMBOLS` 去重（≠×2→×1，≡×2→×1，∫×3→×1），71→67 唯一字符。
- 真相文件压实：补全 06-02 批 11 个遗漏 LaTeX→Unicode 映射文档、按代码实测整理分类表（中文标点19、英文标点6、数学26、希腊1、度量箭头15）、对齐 `resolveBoardTextDisplayRoute` 分流入口说明。不造新东西，只压实已有代码事实。
- 真正文件清单确认：正式主线 StagePreview→LegacyStagePreview→DrawboardStage；目标验证线 KonvaProofPage（不消费 hideAtMs，纯 reveal 验证）；画布渲染层 boardSticker/boardControlLayers/boardTiming/boardReveal。

## B/C 职责边界确认

- 夏夏明确：**B 只管每个语音切片对应的 C 在画布上呆多久，其他不管。**
- B 域字段：`startMs`、`endMs`、`hideAtMs`、`sourceStartMs`、`sourceEndMs`、`sourceRef` — 全是时间窗口。
- C 域字段（五可调）：`xPercent/yPercent/widthPercent`（位置大小）、`fontSize`（字体大小）、`drawSpeed`（书写速度）、内容（boardSlice）。

## 项目理解重大纠偏

- **之前错误认知**：C 内容金手指完全缺失，无编辑入口。
- **正确事实**：C 内容（boardSlice）在 **第二步 ScriptAgent** 就已决定。`ScriptAgentTableEditor` 的 boardSlice 列就是用户内容编辑入口。用户改 boardSlice → compiler 投影到 C 内容。
- **chainKey → 站位规则**：`template-open`→problem(题目区，top 2.4%) / `template-pre`→analysis(分析区，24%) / `step-N`→solution(解答区，右半区) / `template-end`→summary(总结区，74%)。四区边界在 `COURSEWARE_ZONE_BOUNDS`。
- C 不发明内容、不决定站位 — C 只按 zone 自动站位 + 按时 render 上游已决定的 boardSlice。
- C 字体 per-clip 独立字段尚有缺口（当前仅项目级 boardFontFamily/Url）。
- 当前 `TimelineClip` 虽然 B/C 字段扁平混放，但语义边界已清晰：不改数据结构，用认知边界管住即可。

## 工程纪律入骨

- 夏夏要求烙入记忆：**每个值必须有来路去处**。改代码前四问：(1)值从哪一环继承？(2)当前层有没有重新定义第二套？(3)DOM/Canvas/Konva/录制是否都在消费同一个来源？(4)如果不是继承来的还算不算workflow？
- 审查标准：哪里来 → 用途 → 传递什么参数 → 承接来源。运行逻辑有且只能有一个。答不清楚来源的值不允许进入主线。
- 已写入项目 MEMORY.md 和用户级 MEMORY.md。
- 对照 Claude 旧工作文档纠正了做事方式差距：动手前四问、第四步只消费不发明、不信任印象先落足迹、节点完成立刻写摘要。

## 画布双标签去重

- 问题：drawCoursewareStageFrame 的 canvas 硬编码标签 + DrawboardStage 的 DOM CSS 标签 → 页面可见两套。
- 修复：删除 drawCoursewareStageFrame 中的 canvas label 绘制（drawLabel ×4）和 canvas problem 文本绘制（drawProblemSummary）。清理 4 个死函数（drawLabel/drawProblemSummary/wrapCanvasText/roundedRect）。
- 理由：DOM 标签用 CSS 变量驱动→单源真相；canvas 硬编码 → 第二套真相。删硬编码的。
- 保留：strokeRect 画布边框（录制底板需要）。
- tsc ✅。
